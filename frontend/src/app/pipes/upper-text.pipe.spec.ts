import { UpperTextPipe } from './upper-text.pipe';

describe('UpperTextPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperTextPipe();
    expect(pipe).toBeTruthy();
  });
});
